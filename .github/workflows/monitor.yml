name: Real App Monitor

on:
  workflow_dispatch:
  schedule:
    - cron: '30 5 * * *' # 한국 14:30

jobs:
  monitor:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'

    - name: Install Dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y tesseract-ocr tesseract-ocr-kor libtesseract-dev net-tools

    - name: Install Libraries
      run: |
        pip install pandas uiautomator2 gspread oauth2client pytesseract Pillow

    - name: Setup SSH Tunnel (SOCKS5)
      env:
        ORACLE_KEY: ${{ secrets.ORACLE_KEY }}
        ORACLE_HOST: ${{ secrets.ORACLE_HOST }}
        ORACLE_USER: ${{ secrets.ORACLE_USER }}
      run: |
        mkdir -p ~/.ssh
        printf "%s\n" "$ORACLE_KEY" > ~/.ssh/id_rsa
        chmod 600 ~/.ssh/id_rsa
        
        echo "🔌 SSH 터널 연결 (SOCKS5)..."
        ssh -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null -o ExitOnForwardFailure=yes -4 -D 0.0.0.0:8080 -f -C -N -i ~/.ssh/id_rsa $ORACLE_USER@$ORACLE_HOST
        sleep 5

    - name: 🛡️ [진단] 호스트 IP 확인
      run: |
        echo "🌐 GitHub 호스트 IP 검사..."
        PROXY_IP=$(curl -s --socks5 127.0.0.1:8080 https://ipinfo.io/json)
        echo "   👉 결과: $PROXY_IP"
        if [[ "$PROXY_IP" == *"KR"* ]] || [[ "$PROXY_IP" == *"Korea"* ]]; then
          echo "✅ [호스트 성공] 서버 터널은 확실히 뚫렸습니다."
        else
          echo "❌ [호스트 실패] 서버 설정 문제이거나 오라클 방화벽 문제입니다."
          # 실패해도 에뮬레이터 로그를 보기 위해 계속 진행
        fi

    - name: Enable KVM
      run: |
        echo 'KERNEL=="kvm", GROUP="kvm", MODE="0666", OPTIONS+="static_node=kvm"' | sudo tee /etc/udev/rules.d/99-kvm4all.rules
        sudo udevadm control --reload-rules
        sudo udevadm trigger --name-match=kvm

    - name: Run Android Emulator & Script
      uses: reactivecircus/android-emulator-runner@v2
      with:
        # ★ 안정적인 API 30 (Android 11) 사용
        api-level: 30
        target: google_apis
        arch: x86_64
        profile: Nexus 6
        ram-size: 2048M
        heap-size: 512M
        # 프록시 설정 유지
        emulator-options: -no-window -gpu swiftshader_indirect -noaudio -no-boot-anim -camera-back none -dns-server 8.8.8.8 -no-snapshot-load -http-proxy socks5://127.0.0.1:8080 -prop persist.sys.language=ko -prop persist.sys.country=KR -prop persist.sys.locale=ko-KR
        
        script: |
          echo "🚀 안드로이드 부팅 완료. 인터넷 연결 대기 중..."
          
          # 인터넷 연결될 때까지 핑 테스트 (최대 60초 대기)
          timeout 60s bash -c 'until adb shell ping -c 1 8.8.8.8 > /dev/null 2>&1; do echo "   ...인터넷 연결 대기 중"; sleep 2; done'
          echo "✅ 안드로이드 인터넷 연결됨!"
          
          mkdir -p screenshots
          python -u main.py

      env:
        GH_TOKEN: ${{ github.token }}
        G_SHEET_KEY: ${{ secrets.G_SHEET_KEY }}
        G_SHEET_ID: ${{ secrets.G_SHEET_ID }}

    - name: Upload Screenshots
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: debug-screenshots
        path: screenshots/
