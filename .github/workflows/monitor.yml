name: Real App Monitor

on:
  workflow_dispatch:
  schedule:
    - cron: '30 5 * * *' # 한국 14:30

jobs:
  monitor:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'

    - name: Install Dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y tesseract-ocr tesseract-ocr-kor libtesseract-dev net-tools

    - name: Install Libraries
      run: |
        pip install pandas uiautomator2 gspread oauth2client pytesseract Pillow

    - name: Setup SSH Tunnel (SOCKS5)
      env:
        ORACLE_KEY: ${{ secrets.ORACLE_KEY }}
        ORACLE_HOST: ${{ secrets.ORACLE_HOST }}
        ORACLE_USER: ${{ secrets.ORACLE_USER }}
      run: |
        mkdir -p ~/.ssh
        printf "%s\n" "$ORACLE_KEY" > ~/.ssh/id_rsa
        chmod 600 ~/.ssh/id_rsa
        
        echo "🔌 SSH 터널 연결 시도 (SOCKS5 Proxy)..."
        # -o ExitOnForwardFailure=yes: 터널 실패하면 바로 에러 띄움
        ssh -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null -o ExitOnForwardFailure=yes -4 -D 8080 -f -C -N -i ~/.ssh/id_rsa $ORACLE_USER@$ORACLE_HOST -v
        
        echo "⏳ 터널 안정화 대기 (5초)..."
        sleep 5

    - name: 🛡️ [진단] 호스트 IP 확인 (여기서 실패하면 중단)
      run: |
        echo "🌐 GitHub Runner에서 프록시 테스트 중..."
        # 1. 프록시 없이 확인 (미국 IP 나와야 정상)
        echo "   👉 내 원래 IP: $(curl -s https://ipinfo.io/json | grep ip)"
        
        # 2. 프록시 통해서 확인 (한국 IP 나와야 성공)
        PROXY_IP=$(curl -s --socks5-hostname 127.0.0.1:8080 https://ipinfo.io/json)
        echo "   👉 프록시 적용 IP 데이터: $PROXY_IP"
        
        if [[ "$PROXY_IP" == *"KR"* ]] || [[ "$PROXY_IP" == *"Korea"* ]]; then
          echo "✅ [성공] 터널이 완벽하게 뚫렸습니다! 한국 IP입니다."
        else
          echo "❌ [실패] 여전히 한국 IP가 아닙니다. 서버 설정을 다시 확인해야 합니다."
          echo "   (응답 내용: $PROXY_IP)"
          exit 1 # 여기서 강제로 멈춤 (에뮬레이터 실행 X)
        fi

    - name: Enable KVM
      run: |
        echo 'KERNEL=="kvm", GROUP="kvm", MODE="0666", OPTIONS+="static_node=kvm"' | sudo tee /etc/udev/rules.d/99-kvm4all.rules
        sudo udevadm control --reload-rules
        sudo udevadm trigger --name-match=kvm

    - name: Run Android Emulator & Script
      uses: reactivecircus/android-emulator-runner@v2
      with:
        api-level: 33
        target: google_apis
        arch: x86_64
        profile: Nexus 6
        ram-size: 2048M
        heap-size: 512M
        # ★ 중요: socks5:// 명시
        emulator-options: -no-window -gpu swiftshader_indirect -noaudio -no-boot-anim -camera-back none -dns-server 8.8.8.8 -no-snapshot-load -http-proxy socks5://127.0.0.1:8080 -prop persist.sys.language=ko -prop persist.sys.country=KR -prop persist.sys.locale=ko-KR
        
        script: |
          echo "🚀 안드로이드 부팅 완료."
          mkdir -p screenshots
          python -u main.py

      env:
        GH_TOKEN: ${{ github.token }}
        G_SHEET_KEY: ${{ secrets.G_SHEET_KEY }}
        G_SHEET_ID: ${{ secrets.G_SHEET_ID }}

    - name: Upload Screenshots
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: debug-screenshots
        path: screenshots/
