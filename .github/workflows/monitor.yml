name: Real App Monitor

on:
  workflow_dispatch:
  schedule:
    - cron: '30 5 * * *' # 한국 시간 14:30

jobs:
  monitor:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'

    - name: Install Dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y tesseract-ocr tesseract-ocr-kor libtesseract-dev net-tools privoxy speedtest-cli

    - name: Configure Privoxy
      run: |
        echo 'forward-socks5t / 127.0.0.1:8080 .' | sudo tee -a /etc/privoxy/config
        sudo service privoxy restart

    - name: Install Libraries
      run: |
        pip install pandas uiautomator2 adbutils gspread oauth2client pytesseract Pillow

    - name: Setup SSH Tunnel
      env:
        ORACLE_KEY: ${{ secrets.ORACLE_KEY }}
        ORACLE_HOST: ${{ secrets.ORACLE_HOST }}
        ORACLE_USER: ${{ secrets.ORACLE_USER }}
      run: |
        mkdir -p ~/.ssh
        printf "%s\n" "$ORACLE_KEY" > ~/.ssh/id_rsa
        chmod 600 ~/.ssh/id_rsa
        ssh -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null -o ExitOnForwardFailure=yes -4 -D 8080 -f -C -N -i ~/.ssh/id_rsa $ORACLE_USER@$ORACLE_HOST
        sleep 5

    - name: 🛡️ [진단] 네트워크 속도 및 IP 확인
      run: |
        HTTP_IP=$(curl -s -x http://127.0.0.1:8118 https://ipinfo.io/json)
        echo "   👉 Privoxy IP 정보: $HTTP_IP"
        if [[ "$HTTP_IP" == *"KR"* ]] || [[ "$HTTP_IP" == *"Korea"* ]]; then
          echo "✅ [성공] 한국 IP 확인됨!"
        else
          echo "❌ [실패] 한국 IP가 아닙니다."
          exit 1
        fi

    - name: Enable KVM
      run: |
        echo 'KERNEL=="kvm", GROUP="kvm", MODE="0666", OPTIONS+="static_node=kvm"' | sudo tee /etc/udev/rules.d/99-kvm4all.rules
        sudo udevadm control --reload-rules
        sudo udevadm trigger --name-match=kvm

    - name: Run Android Emulator (API 33 - Android 13)
      uses: reactivecircus/android-emulator-runner@v2
      with:
        api-level: 33
        target: google_apis
        arch: x86_64
        # ★ [수정] Pixel 4 -> Nexus 6 (호환성 확보)
        profile: Nexus 6
        ram-size: 4096M
        heap-size: 1024M
        # 서울 시간대 + DNS 고정 + 한국어
        emulator-options: -no-window -gpu swiftshader_indirect -noaudio -no-boot-anim -camera-back none -no-snapshot-load -http-proxy http://127.0.0.1:8118 -timezone Asia/Seoul -dns-server 8.8.8.8
        
        script: |
          echo "🚀 Android 13 (Nexus 6) Booted."
          
          echo "⏰ [필수] 시간 강제 동기화..."
          adb shell settings put global auto_time 0
          adb shell settings put global time_zone Asia/Seoul
          
          HOST_TIME=$(date +%m%d%H%M.%S)
          adb shell date $HOST_TIME
          
          echo "💤 최신 OS 안정화 대기 (90초)..."
          sleep 90
          
          echo "🔓 화면 깨우기..."
          adb shell input keyevent 82
          
          echo "🏃‍♂️ 파이썬 스크립트 실행..."
          mkdir -p screenshots
          python -u main.py

      env:
        GH_TOKEN: ${{ github.token }}
        G_SHEET_KEY: ${{ secrets.G_SHEET_KEY }}
        G_SHEET_ID: ${{ secrets.G_SHEET_ID }}

    - name: Upload Screenshots
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: debug-screenshots
        path: screenshots/
